#include <stdio.h>

int main ()
{
	// printf("texto aquí"): es una función que nos permite imprimir o generar salida de datos
	printf("Hola, gente, espero que se encuentren bien!");

	/* Especificamos el valor de retorno de la función main /
	   finalización del algoritmo o programa
	*/
	return 0;
}
